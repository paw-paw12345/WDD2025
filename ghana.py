# Simple calculator to add two numbers

# Get numbers from user (input returns a string, so convert to float)
num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))

# Add them
result = num1 + num2

# Show the result
print("The sum is:", result)
